# Open Terminal TODO

更新：2026-09-12。8 项已完成或定案，2 项移出当前范围。测试镜像已验收，尚未替换生产服务。

## 当前设计

- 所有用户使用同一套资源上限；活跃用户预留完整额度，不按实时占用动态分配。
- 公开配置仅保留资源预算、任务数上限、总时限及执行后端路径；Swap 固定禁用，队列、清理、去重和线程默认值不再提供调优配置。
- 保留全局／单用户任务数上限和有界公平队列，不增加持久化排队 API。
- 三入口统一使用运行总时限，包含空闲时间，不支持续期。
- 终端 WebSocket 断连即清理，不保留后台会话供重连。
- 文件辅助进程共享有界并发池，不增加按用户调度。
- 创建去重仅在当前进程有效；失败清理完成后允许原 key 重试，清理无法确认时保留 key。

## 移出当前范围

| 编号 | 项目 | 当前边界 |
| --- | --- | --- |
| OT-008 | 任务持久化与重启恢复 | 不恢复任务、队列或幂等记录；启动发现残留任务时拒绝启动，要求先处理残留。 |
| OT-010 | 完整资源指标 | 保留已有状态、时间和结束原因；暂不增加持久化指标、资源峰值与历史查询。 |

## 已完成或定案

| 编号 | 完成日期 | 结果与验收依据 |
| --- | --- | --- |
| OT-001 | 2026-09-11 | Notebook 会话、上下文、路径与内核身份隔离；真实双用户越权、符号链接、读写归属及停止回收验收通过，覆盖 Jupyter Client 8.0.0。[测试](tests/notebook_container_smoke.py) |
| OT-002 | 2026-09-11 | Docker 构建和 manifest 统一使用小写仓库名；含大写组织名的输入验证通过，未重新启用自动发布。[工作流](.github/workflows/docker.yml) |
| OT-003 | 2026-09-12 | 命令、PTY、Notebook 共享一份用户 CPU、内存、PID／Swap 限额，任务分组仅用于生命周期管理；服务、辅助进程和计算池分别预算。真实 cgroup 验收通过。 |
| OT-004 | 2026-09-12 | 文件辅助进程使用有界并发与队列、独立 helpers 预算；计算负载下并发上传、内容校验和取消回收通过。 |
| OT-005 | 2026-09-12 | 三入口运行总时限、两阶段终止和结束原因；忽略 SIGTERM、setsid、父进程先退出等场景完整回收通过。 |
| OT-006 | 2026-09-12 | 三入口计算线程默认值与覆盖规则统一；真实环境验证通过，自建线程仍受用户合计硬限额约束。 |
| OT-007 | 2026-09-12 | 有界公平队列与进程内创建去重完成；失败清理完成后释放 key，清理期间重试不重复创建，未确认清理的错误保留记录；全仓 212 项回归通过。 |
| OT-009 | 2026-09-12 | 终端断连即清理，沿用现有行为；不实现持续会话与重连恢复。 |

精简配置后，`open-terminal:fixed-config-20260912` 已通过真实内核、计算线程与 HTTP 验收，覆盖用户合计上限、零 Swap、12 路上传及三入口超时回收。首次并发运行验收套件时有上传触发辅助进程等待超时并返回 `503`，单独复测通过；过载仍允许拒绝请求。全仓 212 项回归通过，复测脚本保留在 [tests/](tests/)。

配置与使用限制见 [README](README.md#resource-limits)。默认 `legacy` 模式不提供计算资源硬限制；跨重启恢复不在当前范围。
